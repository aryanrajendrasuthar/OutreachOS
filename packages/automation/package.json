{
  "name": "@outreachos/automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright LinkedIn automation engine for OutreachOS",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@outreachos/shared": "*",
    "pino": "^9.1.0",
    "playwright": "^1.44.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.5.0",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
